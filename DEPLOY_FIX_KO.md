# 배포 화면이 그대로 보일 때: 비개발자용 붙여넣기 안내

현재 페이지 상단에 `diff --git a/index.html ...` 같은 문구가 보이거나, 로그인 화면이 나오지 않는다면 대부분 아래 둘 중 하나입니다.

1. **변경내역(diff)을 웹페이지 코드처럼 붙여넣은 경우**
2. **새 `index.html`이 배포되지 않았거나, 브라우저에 이전 로그인 상태/캐시가 남은 경우**

## 1) 가장 안전한 수정 방법

배포 사이트 편집 화면에서 기존 `index.html` 내용을 전부 지우고, 저장소의 `index.html` 파일 내용을 **처음부터 끝까지 통째로 복사해서 붙여넣으세요.**

반드시 파일의 첫 줄은 아래처럼 시작해야 합니다.

```html
<!doctype html>
<html lang="ko">
```

아래처럼 시작하면 잘못 붙여넣은 것입니다.

```diff
diff --git a/index.html b/index.html
index 9c22763...052b668 100644
--- a/index.html
+++ b/index.html
```

위 `diff --git`, `index ...`, `--- a/index.html`, `+++ b/index.html`, `@@ ...` 줄은 웹페이지 코드가 아니라 변경 설명입니다. 절대 붙여넣지 마세요.

## 2) 로그인 화면이 안 보일 때

이미 관리자/이용자로 로그인된 브라우저라면 로그인 화면이 숨겨질 수 있습니다. 배포 주소 뒤에 아래처럼 붙여서 접속하세요.

```text
?logout=1
```

예시:

```text
https://내-배포주소/index.html?logout=1
```

그러면 저장된 로그인 상태가 지워지고 로그인 화면이 다시 보입니다.

## 3) 배포가 제대로 바뀌었는지 확인

정상 적용된 화면 상단 설명 끝에 `로그인 적용 버전` 배지가 보입니다.

또한 첫 화면에는 `접근 코드 입력`, `이용자 로그인`, `관리자 로그인`이 보여야 합니다.

## 4) 화면에 `+`, `-` 기호가 돌아다니고 버튼이 안 눌릴 때

이 증상은 거의 항상 **diff 화면을 복사해서 붙여넣은 경우**입니다. diff 화면의 `+`와 `-`는 “추가/삭제된 줄” 표시이지 실제 웹페이지 코드가 아닙니다.

해결 방법은 일부 줄만 고치는 것이 아니라, 배포 화면의 `index.html`을 전부 지우고 저장소의 `index.html` 원본 전체를 다시 붙여넣는 것입니다.

붙여넣기 전에 아래 내용을 확인하세요.

- 붙여넣는 첫 글자가 `<!doctype html>` 이어야 합니다.
- 붙여넣는 내용 안에 `diff --git`, `--- a/index.html`, `+++ b/index.html`, `@@`가 있으면 안 됩니다.
- 저장 후 브라우저 주소 뒤에 `?logout=1`을 붙여 새로고침합니다.

## 5) “바로 붙여넣기용” 파일

바로 복사할 수 있는 안내와 응급 코드는 `COPY_PASTE_GUIDE_KO.md` 파일에 따로 정리했습니다.

- 가장 안전한 해결: `index.html` 원본 전체를 복사해서 기존 배포 파일 전체와 교체
- 임시 응급 해결: `</body>` 바로 위에 붙여넣는 diff 표시 제거 코드 사용
